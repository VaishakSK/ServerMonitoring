<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KLE Tech University - CAIR Lab Server Monitoring</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="/css/theme-toggle.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            overflow-y: auto;
            width: 100%;
            height: 100%;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: #0a0a0a;
            color: white;
            position: relative;
            min-height: 100vh;
        }
        
        /* Light Theme Styles */
        [data-theme="light"] body {
            background: #ffffff;
            color: #333;
        }
        
        [data-theme="light"] .navbar {
            background: rgba(255, 255, 255, 0.1);
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        [data-theme="light"] .overlay {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(255, 255, 255, 0.6) 100%);
        }
        
        [data-theme="light"] .hero-title,
        [data-theme="light"] .hero-subtitle {
            color: #333;
        }
        
        [data-theme="light"] .feature-item {
            background: rgba(255, 255, 255, 0.9);
            color: #333;
        }
        
        [data-theme="light"] .auth-container {
            background: rgba(255, 255, 255, 0.95);
            color: #333;
        }

        /* Main Container */
        .main-container {
            position: relative;
            width: 100vw;
            min-height: 100vh;
            overflow-x: hidden;
            overflow-y: auto;
        }

        /* Background Image */
        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            z-index: -2;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.8) 0%, rgba(0, 0, 0, 0.6) 100%);
            z-index: -1;
        }

        /* Navigation */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px 40px;
            background: rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
            color: white;
            text-decoration: none;
        }

        .logo-icon {
            width: 45px;
            height: 45px;
            background: linear-gradient(135deg, #00d4ff, #014c65);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            box-shadow: 0 8px 25px rgba(0, 212, 255, 0.3);
        }

        .logo-image {
            height: 45px;
            width: auto;
            object-fit: contain;
        }

        /* Main Content Layout */
        .content-wrapper {
            position: relative;
            z-index: 10;
            width: 100%;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 100px 40px 40px 40px;
            box-sizing: border-box;
        }

        .content-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            max-width: 1400px;
            width: 100%;
            height: 100%;
            align-items: center;
            box-sizing: border-box;
        }

        /* Left Section - Information */
        .left-section {
            display: flex;
            flex-direction: column;
            gap: 25px;
            max-width: 580px;
            height: 100%;
            justify-content: center;
            box-sizing: border-box;
        }

        .hero-title {
            font-size: clamp(2.2rem, 3.5vw, 3rem);
            font-weight: 800;
            line-height: 1.1;
            background: linear-gradient(135deg, #ffffff 0%, #00d4ff 50%, #0099cc 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 15px;
        }

        .hero-subtitle {
            font-size: clamp(0.9rem, 1.3vw, 1.1rem);
            font-weight: 400;
            line-height: 1.5;
            opacity: 0.9;
            margin-bottom: 20px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 18px;
            margin-top: 15px;
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            gap: 12px;
            padding: 16px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 14px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            transition: all 0.3s ease;
        }

        .feature-item:hover {
            transform: translateY(-3px);
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(0, 212, 255, 0.3);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
        }

        .feature-icon {
            width: 42px;
            height: 42px;
            background: linear-gradient(135deg, #00d4ff, #0099cc);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            flex-shrink: 0;
        }

        .feature-content h3 {
            font-size: 1rem;
            font-weight: 600;
            margin-bottom: 6px;
            color: #00d4ff;
        }

        .feature-content p {
            font-size: 0.85rem;
            line-height: 1.4;
            opacity: 0.8;
        }

        /* Right Section - Authentication */
        .right-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            max-width: 480px;
            height: 100%;
        }

        .auth-container {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(30px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 40px 35px;
            width: 100%;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }

        .auth-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #00d4ff, #0099cc, #533483);
            animation: borderFlow 3s linear infinite;
        }

        @keyframes borderFlow {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        .auth-title {
            font-size: clamp(1.6rem, 2.2vw, 1.8rem);
            font-weight: 700;
            margin-bottom: 12px;
            color: white;
        }

        .auth-subtitle {
            font-size: clamp(0.8rem, 1vw, 0.9rem);
            opacity: 0.7;
            margin-bottom: 30px;
            line-height: 1.4;
        }

        .auth-buttons {
            display: flex;
            flex-direction: column;
            gap: 16px;
            width: 100%;
        }

        .auth-btn {
            padding: clamp(14px, 1.8vw, 16px) clamp(24px, 2.5vw, 28px);
            border: none;
            border-radius: 14px;
            font-size: clamp(0.9rem, 1.1vw, 1rem);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            position: relative;
            overflow: hidden;
            width: 100%;
        }

        .auth-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .auth-btn:hover::before {
            left: 100%;
        }

        .btn-primary {
            background: linear-gradient(135deg, #00d4ff, #0099cc);
            color: white;
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(0, 212, 255, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.5);
            transform: translateY(-3px);
        }

        /* Responsive Design */
        @media (max-width: 1400px) {
            .content-container {
                gap: 50px;
                max-width: 1200px;
            }
            
            .left-section {
                max-width: 520px;
            }
            
            .right-section {
                max-width: 440px;
            }
        }

        @media (max-width: 1200px) {
            .content-container {
                gap: 45px;
                max-width: 1000px;
            }
            
            .hero-title {
                font-size: clamp(2rem, 3vw, 2.5rem);
            }
            
            .features-grid {
                gap: 16px;
            }
            
            .feature-item {
                padding: 15px;
            }
            
            .left-section {
                max-width: 500px;
            }
            
            .right-section {
                max-width: 420px;
            }
        }

        @media (max-width: 992px) {
            .content-container {
                grid-template-columns: 1fr;
                gap: 35px;
                text-align: center;
                max-width: 700px;
            }
            
            .left-section {
                order: 2;
                max-width: 100%;
                height: auto;
                gap: 25px;
            }
            
            .right-section {
                order: 1;
                max-width: 100%;
                height: auto;
            }
            
            .features-grid {
                grid-template-columns: 1fr;
                max-width: 450px;
                margin: 0 auto;
            }
            
            .content-wrapper {
                padding: 100px 30px 40px 30px;
            }
            
            .hero-title {
                font-size: clamp(2.2rem, 4vw, 2.8rem);
            }
            
            .hero-subtitle {
                font-size: clamp(1rem, 1.5vw, 1.2rem);
            }
        }

        @media (max-width: 768px) {
            .navbar {
                padding: 15px 20px;
            }
            
            .logo-image {
                height: 40px;
            }
            
            .logo-icon {
                width: 40px;
                height: 40px;
                font-size: 18px;
            }
            
            .content-wrapper {
                padding: 100px 20px 30px 20px;
            }
            
            .hero-title {
                font-size: clamp(1.8rem, 4vw, 2.2rem);
            }
            
            .hero-subtitle {
                font-size: clamp(0.8rem, 2.5vw, 0.9rem);
            }
            
            .features-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }
            
            .feature-item {
                padding: 14px;
            }
            
            .feature-icon {
                width: 40px;
                height: 40px;
                font-size: 18px;
            }
            
            .feature-content h3 {
                font-size: 1rem;
            }
            
            .feature-content p {
                font-size: 0.85rem;
            }
            
            .auth-container {
                padding: 35px 25px;
            }
            
            .auth-title {
                font-size: clamp(1.4rem, 4vw, 1.6rem);
            }
            
            .auth-subtitle {
                font-size: clamp(0.8rem, 2.5vw, 0.9rem);
            }
            
            .content-container {
                grid-template-columns: 1fr;
                gap: 30px;
            }
            
            .left-section {
                order: 2;
                max-width: 100%;
                text-align: center;
            }
            
            .right-section {
                order: 1;
                max-width: 100%;
            }
            
            .auth-buttons {
                flex-direction: column;
                gap: 15px;
            }
            
            .auth-btn {
                width: 100%;
                text-align: center;
            }
        }

        @media (max-width: 480px) {
            .navbar {
                padding: 12px 15px;
            }
            
            .logo-image {
                height: 35px;
            }
            
            .logo-icon {
                width: 35px;
                height: 35px;
                font-size: 16px;
            }
            
            .content-wrapper {
                padding: 100px 15px 25px 15px;
            }
            
            .hero-title {
                font-size: clamp(1.5rem, 5vw, 1.8rem);
            }
            
            .hero-subtitle {
                font-size: clamp(0.8rem, 3vw, 0.9rem);
            }
            
            .features-grid {
                gap: 12px;
            }
            
            .feature-item {
                padding: 12px;
            }
            
            .feature-icon {
                width: 38px;
                height: 38px;
                font-size: 17px;
            }
            
            .feature-content h3 {
                font-size: 0.95rem;
            }
            
            .feature-content p {
                font-size: 0.8rem;
            }
            
            .auth-container {
                padding: 30px 20px;
            }
            
            .auth-title {
                font-size: clamp(1.3rem, 4vw, 1.5rem);
            }
            
            .auth-subtitle {
                font-size: clamp(0.8rem, 2.5vw, 0.9rem);
            }
            
            .content-container {
                gap: 25px;
            }
        }

        @media (max-width: 360px) {
            .content-wrapper {
                padding: 100px 10px 20px 10px;
            }
            
            .hero-title {
                font-size: 1.5rem;
            }
            
            .hero-subtitle {
                font-size: 0.8rem;
            }
            
            .feature-item {
                padding: 10px;
            }
            
            .feature-icon {
                width: 35px;
                height: 35px;
                font-size: 16px;
            }
            
            .auth-container {
                padding: 25px 15px;
            }
            
            .content-container {
                gap: 20px;
            }
        }

        /* Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        .fade-in:nth-child(1) { animation-delay: 0.1s; }
        .fade-in:nth-child(2) { animation-delay: 0.3s; }
        .fade-in:nth-child(3) { animation-delay: 0.5s; }
        .fade-in:nth-child(4) { animation-delay: 0.7s; }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Floating Elements */
        .floating-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .floating-circle {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(45deg, rgba(0, 212, 255, 0.1), rgba(0, 153, 204, 0.05));
            animation: float 20s infinite linear;
        }

        .circle-1 {
            width: 200px;
            height: 200px;
            top: 20%;
            right: 10%;
            animation-delay: 0s;
        }

        .circle-2 {
            width: 150px;
            height: 150px;
            bottom: 30%;
            left: 5%;
            animation-delay: -10s;
        }

        @keyframes float {
            0% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
            100% { transform: translateY(0px) rotate(360deg); }
        }

        /* Scroll Down Button */
        .scroll-down-btn {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #00d4ff, #0099cc);
            border: none;
            border-radius: 50%;
            color: white;
            font-size: 24px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.3);
            z-index: 1001;
            display: none;
            align-items: center;
            justify-content: center;
            animation: bounce 2s infinite;
        }

        .scroll-down-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 212, 255, 0.4);
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        /* Show scroll button only on mobile/tablet */
        @media (max-width: 992px) {
            .scroll-down-btn {
                display: flex;
            }
            
            .content-wrapper {
                min-height: 100vh;
                height: auto;
                padding-bottom: 100px;
            }
            
            .content-container {
                height: auto;
                min-height: 100vh;
            }
            
            .left-section, .right-section {
                height: auto;
                min-height: auto;
            }
        }

        @media (max-width: 768px) {
            .scroll-down-btn {
                bottom: 20px;
                right: 20px;
                width: 50px;
                height: 50px;
                font-size: 20px;
            }
        }

        @media (max-width: 480px) {
            .scroll-down-btn {
                bottom: 15px;
                right: 15px;
                width: 45px;
                height: 45px;
                font-size: 18px;
            }
        }

        /* Footer */
        .footer {
            position: relative;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(20px);
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding: 30px 0;
            margin-top: auto;
            z-index: 100;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 40px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            align-items: start;
        }

        .footer-section {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .footer-section h3 {
            color: #00d4ff;
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .footer-section p,
        .footer-section a {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.9rem;
            line-height: 1.5;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-section a:hover {
            color: #00d4ff;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 20px;
            margin-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.85rem;
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 10px;
        }

        .social-link {
            width: 35px;
            height: 35px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .social-link:hover {
            background: #00d4ff;
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .footer {
                padding: 25px 0;
            }
            
            .footer-content {
                padding: 0 20px;
                grid-template-columns: 1fr;
                gap: 30px;
                text-align: center;
            }
            
            .footer-section {
                gap: 12px;
            }
            
            .footer-section h3 {
                font-size: 1rem;
                margin-bottom: 8px;
            }
            
            .footer-section p,
            .footer-section a {
                font-size: 0.85rem;
            }
            
            .social-links {
                justify-content: center;
                gap: 12px;
            }
            
            .social-link {
                width: 32px;
                height: 32px;
            }
        }

        @media (max-width: 480px) {
            .footer {
                padding: 20px 0;
            }
            
            .footer-content {
                padding: 0 15px;
                gap: 25px;
            }
            
            .footer-section {
                gap: 10px;
            }
            
            .footer-section h3 {
                font-size: 0.95rem;
            }
            
            .footer-section p,
            .footer-section a {
                font-size: 0.8rem;
            }
            
            .social-links {
                gap: 10px;
            }
            
            .social-link {
                width: 30px;
                height: 30px;
            }
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 12px;
        }

        ::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 6px;
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, #00d4ff, #0099cc);
            border-radius: 6px;
            border: 2px solid rgba(0, 0, 0, 0.3);
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, #00b8e6, #0088b3);
        }

        /* Firefox scrollbar */
        html {
            scrollbar-width: thin;
            scrollbar-color: #00d4ff rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body>
    <!-- Background Image -->
    <img src="/images/loginbackgroundimage.jpg" alt="Server Monitoring" class="background-image">
    <div class="overlay"></div>

    <!-- Floating Elements -->
    <div class="floating-elements">
        <div class="floating-circle circle-1"></div>
        <div class="floating-circle circle-2"></div>
    </div>

    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-content">
            <a href="/" class="logo">
                <div class="logo-icon">🖥️</div>
                <img src="/images/logo.png" alt="KLE Tech Logo" class="logo-image">
            </a>
            
            <!-- Theme Toggle -->
                            {{themeToggle}}
        </div>
    </nav>

    <!-- Main Content -->
    <div class="main-container">
        <div class="content-wrapper">
            <div class="content-container">
                <!-- Left Section - Information -->
                <div class="left-section">
                    <h1 class="hero-title fade-in">Server Monitoring System</h1>
                    <p class="hero-subtitle fade-in">
                        Advanced monitoring and management platform for KLE Technological University's 
                        CAIR Lab cloud computing and networking infrastructure
                    </p>
                    
                    <div class="features-grid fade-in">
                        <div class="feature-item">
                            <div class="feature-icon">☁️</div>
                            <div class="feature-content">
                                <h3>Cloud Computing</h3>
                                <p>Monitor cloud infrastructure and virtual machines with real-time analytics</p>
                            </div>
                        </div>
                        
                        <div class="feature-item">
                            <div class="feature-icon">🌐</div>
                            <div class="feature-content">
                                <h3>Networking</h3>
                                <p>Track network performance and connectivity across all lab servers</p>
                            </div>
                        </div>
                        
                        <div class="feature-item">
                            <div class="feature-icon">🔒</div>
                            <div class="feature-content">
                                <h3>Secure Access</h3>
                                <p>Role-based access control with advanced authentication</p>
                            </div>
                        </div>
                        
                        <div class="feature-item">
                            <div class="feature-icon">📊</div>
                            <div class="feature-content">
                                <h3>Real-time Data</h3>
                                <p>Live monitoring and comprehensive reporting system</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Right Section - Authentication -->
                <div class="right-section">
                    <div class="auth-container fade-in">
                        <h2 class="auth-title">Get Started</h2>
                        <p class="auth-subtitle">
                            Access the server monitoring dashboard to manage and monitor 
                            your cloud computing and networking infrastructure
                        </p>
                        
                        <div class="auth-buttons">
                            <a href="/auth" class="auth-btn btn-primary">Login to Dashboard</a>
                            <a href="/auth" class="auth-btn btn-secondary">Create Account</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scroll Down Button (Mobile/Tablet Only) -->
    <button class="scroll-down-btn" id="scrollDownBtn" title="Scroll Down">
        ↓
    </button>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <p class="footer-text">
                © 2025 KLE Technological University - CAIR Lab. 
            </p>
        </div>
    </footer>

    <script>
        // Navbar scroll effect
        window.addEventListener('scroll', () => {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(0, 0, 0, 0.9)';
            } else {
                navbar.style.background = 'rgba(0, 0, 0, 0.1)';
            }
        });

        // Add some interactive effects
        document.addEventListener('mousemove', (e) => {
            const circles = document.querySelectorAll('.floating-circle');
            const x = e.clientX / window.innerWidth;
            const y = e.clientY / window.innerHeight;

            circles.forEach((circle, index) => {
                const speed = (index + 1) * 0.3;
                circle.style.transform = `translate(${x * speed * 15}px, ${y * speed * 15}px)`;
            });
        });

        // Scroll down button functionality
        const scrollDownBtn = document.getElementById('scrollDownBtn');
        
        if (scrollDownBtn) {
            scrollDownBtn.addEventListener('click', () => {
                // Smooth scroll to the bottom of the content
                const contentWrapper = document.querySelector('.content-wrapper');
                if (contentWrapper) {
                    contentWrapper.scrollIntoView({
                        behavior: 'smooth',
                        block: 'end'
                    });
                }
            });
        }

        // Hide scroll button when at bottom (optional)
        window.addEventListener('scroll', () => {
            if (scrollDownBtn && window.innerWidth <= 992) {
                const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                const windowHeight = window.innerHeight;
                const documentHeight = document.documentElement.scrollHeight;
                
                if (scrollTop + windowHeight >= documentHeight - 100) {
                    scrollDownBtn.style.opacity = '0.5';
                } else {
                    scrollDownBtn.style.opacity = '1';
                }
            }
        });
    </script>
    <script src="/js/theme-toggle.js"></script>
</body>
</html>
